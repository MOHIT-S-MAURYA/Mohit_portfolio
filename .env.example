# Environment Variables Configuration
# For secure deployment, set these environment variables instead of hardcoding in settings

# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=true
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password
DEFAULT_FROM_EMAIL=your-email@gmail.com
CONTACT_EMAIL=your-email@gmail.com
EMAIL_TIMEOUT=60

# Admin Configuration
ADMIN_EMAIL=your-admin-email@gmail.com

# Django Configuration (for production)
SECRET_KEY=your-production-secret-key-here
DEBUG=false
ALLOWED_HOSTS=your-domain.com,www.your-domain.com

# Database Configuration (if using PostgreSQL in production)
# DB_NAME=portfolio_db
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_HOST=localhost
# DB_PORT=5432

# How to use:
# 1. Copy this file to .env (if using python-decouple)
# 2. Or export these variables in your shell:
#    export EMAIL_HOST_USER=your-email@gmail.com
#    export EMAIL_HOST_PASSWORD=your-gmail-app-password
# 3. Or set them in your hosting platform's environment variables section
